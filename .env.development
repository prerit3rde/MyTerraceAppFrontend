# Empty on purpose: requests go to relative /api/* paths, which the Vite dev
# server proxy (see vite.config.js) forwards to the real API server-to-server.
# This keeps the browser's request same-origin so CORS never applies in dev.
VITE_API_BASE_URL=
