# Used for `npm run build`. The deployed frontend calls this URL directly, so
# the backend must send Access-Control-Allow-Origin for the deployed origin
# (there is no dev-proxy in a static production build).
VITE_API_BASE_URL=https://api.myterraceapp.com
