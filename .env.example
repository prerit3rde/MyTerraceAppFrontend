# See .env.development / .env.production — Vite loads the file matching the
# current mode (`npm run dev` -> development, `npm run build` -> production)
# automatically, so you normally won't need a plain .env at all.
VITE_API_BASE_URL=https://api.myterraceapp.com
